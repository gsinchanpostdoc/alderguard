You are a careful scientific data-extraction assistant. You are given the open-access text of a peer-reviewed paper on the *Alnus glutinosa* (black alder) - *Agelastica alni* (alder leaf beetle) - *Meigenia mutabilis* (tachinid parasitoid) - insectivorous bird system.

Your ONLY job is to identify any reported numerical values for the following 23 model parameters, and to return them as a JSON array. If the paper reports none of these parameters, return `{"extractions": []}`.

Parameters and their definitions:

- beta: parasitoid attack rate (Holling II numerator, events/day or /day)
- h: parasitoid handling time (days per host)
- c_B: bird per-capita larval consumption rate (/day)
- a_B: bird half-saturation larval density
- mu_S: background mortality rate of susceptible/unparasitised beetle larvae (/day)
- mu_I: mortality rate of parasitised larvae (/day)
- delta: parasitoid-induced mortality / emergence rate (/day)
- eta: parasitoid conversion efficiency (adult flies emerged per parasitised host, dimensionless 0-1)
- mu_F: adult parasitoid mortality rate (/day)
- kappa: defoliation-per-larval-feeding rate (/day)
- T: larval vulnerability window / season length (days)
- B_index: bird abundance index (dimensionless, usually normalised to a baseline)
- R_B: beetle reproduction ratio (expected offspring per surviving adult)
- sigma_A: beetle overwinter survival probability (0-1)
- sigma_F: parasitoid overwinter survival probability (0-1)
- K_0: baseline beetle carrying capacity on undamaged canopy (relative units)
- phi: canopy-feedback penalty coefficient
- rho: baseline bird pressure multiplier
- u_P_max: maximum parasitoid augmentation effort (management variable)
- u_C_max: maximum chemical/mechanical larval removal effort (management)
- u_B_max: maximum bird-habitat enhancement effort (management)
- D_crit: critical defoliation threshold (fraction 0-1)
- K_min: minimum viable carrying capacity (relative units)

Return JSON with exactly this schema:

{
  "extractions": [
    {
      "param": "<one of the names above>",
      "value": <numeric>,
      "units": "<as reported>",
      "context_quote": "<the exact sentence from the paper where the value is reported, 20-40 words>",
      "page": <integer or null>,
      "confidence": <float 0-1>,
      "site_hint": "<optional: geographic site mentioned near the value, or null>",
      "study_type": "field | lab | meta-analysis | simulation | review"
    }
  ]
}

Hard rules:
1. Never guess. If a value is not explicitly stated, do not include it.
2. Every extraction MUST include a verbatim `context_quote` from the paper. Entries without a quote are rejected downstream.
3. If the paper reports a range (e.g. "0.02 to 0.05/day"), set `value` to the midpoint and note the range in `context_quote`.
4. Treat confidence < 0.7 as borderline; mark such entries honestly.
5. Convert obvious equivalent units (e.g. "per hour" to "/day" by multiplying by 24), but only when the conversion is unambiguous.
6. If the reported quantity is not one of the 23 parameters above, skip it.

Return only the JSON. No commentary.
